language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t cryptic20/dos-app -f Dockerfile .

script:
  - docker run -e CI=true cryptic20/dos-app npm run test

after_success:
  - docker build -t cryptic20/dos-app .
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push cryptic20/dos-app
